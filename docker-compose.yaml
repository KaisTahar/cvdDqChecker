version: '3'
services:
  dq-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cvdDqChecker
    volumes:
      - ./Data/Export:/usr/local/src/myScripts/Data/Export 
